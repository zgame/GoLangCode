---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by soonyo.
--- DateTime: 2019/10/22 14:18
---

--------------------------------------------------------------------------------------
--- Message: 玩家的游戏中的聊天数据
--- 这里直接定义成员变量， 但是不写成员函数，就不用reload
--- 这里要注意一点：  当热更新的时候， 所有的已经创建好的player对象是存在的， 结构也是老的结构， 如果你增加了字段，修改了字段，需要进行reload的单独数据处理
--------------------------------------------------------------------------------------
---@class ChatServerMessage
ChatServerMessage = {}

---@return ChatServerMessage
function ChatServerMessage:New(uid)
    local c = {
        dwMsgID = uid,--消息的ID
        tTimeStamp = 0,--时间戳
        dwEmotion = 0, --表情id
        sOriginalContent = "",--原始内容
        sAfterFilterContent = "",--过滤后的内容
    }
    --setmetatable(c, self)
    --self.__index = self
    return c
end

function ChatServerMessage:Reload(o)
    --- 重新刷一次元表,以便调用新定义方法，更新老方法
    G_SetMetaTable(o,self)
    -- 如果热更新有改动成员变量的定义的话， 下面需要进行成员变量的处理
    -- 比如 1 增加了字段， 那么你需要将老数据进行， 新字段的初始化
    -- 比如 2 删除了字段， 那么你需要将老数据进行， 老字段=nil
    -- 比如 3 修改了字段， 那么你需要将老数据进行， 老字段=nil， 新字段初始化或者进行赋值处理
end


